<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      FrogCrypto
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;1,100;1,200;1,300;1,400;1,500;1,600;1,700&family=Noto+Emoji:wght@300..700&display=swap"
      rel="stylesheet"
    />
    <style>
      body { font-family: "IBM Plex Mono", serif; font-weight: 400; font-style:
      normal; } html, body { margin: 0; padding: 0; } * { box-sizing:
      border-box; } .btn { border-radius: 8px; border: 2px solid #FFC971;
      background: linear-gradient(0deg, #FFAE00 0%, #4CCCB0 100%); height:
      calc(100% - 10px); width: 100%; color: #000; display: flex; align-items:
      center; justify-content: center; gap: 8px; /* Fallback for older browsers
      */ color: #154133; text-align: center; font-size: 28px; font-style:
      normal; font-weight: 400; cursor: pointer; box-shadow: 0 8px #A8650F; }
      .btn:active { transform: translateY(5px); box-shadow: 0 3px #8f560d; }
    </style>
  </head>
  <body>
    {{{body}}}
  </body>
</html>